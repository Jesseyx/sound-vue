export const accessToken = state => state.accessToken;

export const authedUser = state => state.user;

export const authedPlaylists = state => state.playlists;

export const authedLikes = state => state.likes;

export const authedFollowings = state => state.followings;
